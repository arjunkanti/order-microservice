server:
  port: 9222

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orders_db
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
